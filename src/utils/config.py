# src/utils/config.py

import os

# Define the project root directory by moving up two levels from the utils directory
PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), "..", ".."))
